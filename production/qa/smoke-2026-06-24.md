# Smoke Check — 2026-06-24 (MVP)

**Verdict: PASS** — критический путь работает, сборка готова к ручному QA/плейтесту.

## Среда
- Godot 4.7.stable.official (установлен: /Applications/Godot.app)
- Проект: /Users/MAKENZI/timokha-escape

## Автоматические проверки

### 1) Парсинг/компиляция всего графа скриптов
`godot --headless --path . --quit-after 20` → **чисто**, без SCRIPT ERROR / parse / compile.
Автозагрузки (GameData, GS, Sfx, Music) инстанцируются; главная сцена грузится в меню.

### 2) Юнит-логика — `tests/run_tests.gd`
`== Result: 23 passed, 0 failed ==`
Покрыто: мини-игры pies/tea/wood (прогресс, завершение, промахи, звёзды),
QuestManager (порядок, средние звёзды), Pursuer (приближение/телеграф/frozen),
сохранение GS (unlocked/best_stars/настройки звука, изолированный save_path).

### 3) Интеграция — `tests/integration_world.gd`
`== Result: 14 passed, 0 failed ==`
**Все 4 главы** пройдены end-to-end в headless: авто-старт мини-игр у зон (pies/tea/wood) →
выполнение всех задач → открытие выхода → достижение выхода → сигнал `finished` со звёздами 1..3.
Путь «осаливания» (поимка шалуном) отработал без краша.

### 4) Геймплей-цикл + отрисовка + SFX
`godot --headless --quit-after 150 -- --autostart` → входит в геймплей, гоняет
`World._process`/`_draw` 150 кадров, проигрывает SFX — без рантайм-ошибок.

### 5) Flow экран-менеджера — `tests/integration_flow.gd`
`== Result: 13 passed, 0 failed ==`
Меню → выбор глав → старт игры → пауза (оверлей) → resume → результаты → настройки → меню.

## Критический путь (ручной чек-лист для плейтеста)
- [x] Меню → ИГРАТЬ → выбор главы (логика разблокировки/звёзд).
- [x] Геймплей: движение (клавиши/тач), шалун постоянно гонится, телеграф рывка.
- [x] Мини-игры у зон (pies/tea/wood), замедление погони у зоны.
- [x] «Осаливание» = сброс прогресса + отскок + оглушение, без Game Over.
- [x] Все задачи → выход → «СБЕЖАЛ!» → результаты со звёздами.
- [x] Сохранение прогресса/разблокировка следующей главы (user://save.json).

## Известные ограничения (не блокеры MVP)
- Арт — плейсхолдеры (фигуры через `_draw`). Звук — процедурные плейсхолдер-SFX +
  зацикленная музыка (`tools/gen_sfx.py`, `tools/gen_music.py`). Всё заменяемо drop-in.
- Экспортные сборки iOS/Android не собраны (нужны платформенные SDK/подписи) — см. launch-notes.
- Сейв не защищён от подмены (приемлемо для премиум-сингла; /security-audit перед релизом).
